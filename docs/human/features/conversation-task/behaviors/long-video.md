---
name: long-video
type: behavior
status: done
owner: human
updated: 2026-08-25
links: []
---

# 长视频分段生成

## 规则

| 当 | 则 |
| --- | --- |
| 源视频不超过 10 秒 | 保持现有单个 H3 多图参考任务，不改变已有结果与付费语义 |
| 源视频超过 10 秒且不超过 300 秒 | 按场景规划连续片段，每段 provider 整秒时长不超过 10 秒，并在确认页显示子任务数量 |
| 查看分段产物 | 每段提示词默认折叠并可反复展开；关键帧默认显示小缩略图，可点击或用 Enter/Space 查看大图，关闭后回到缩略状态 |
| 两段属于同一镜头 | 关闭快速模式时，下一段使用上一段真实生成尾帧作为首帧；开启时，下一段改用前一分段 plan 中 SHA 绑定的冻结末帧 bytes 作为首帧。两种模式都使用当前源片段冻结末帧作为目标尾帧 |
| 边界是硬切 | 新片段从源视频首帧与末帧独立起链，不继承上一镜头尾帧 |
| 当前网页新建长视频 | 固定开启“快速模式”，确认页不显示开关或说明，结果参数摘要也不展示该模式；不读取或等待上一段 `generated.mp4`，相邻 continue 段共享前一分段 plan 中 SHA 绑定的冻结 last-frame bytes，并经同一 fit 处理后作为下一段 first frame，因而可以快速提交所有分段。供应商仍可能排队，不代表其 GPU 同时生成 |
| 兼容调用显式提交 `fast_mode=false` | 保持串行生成顺序：同一链等待上一段完成后再提交下一段，使用上一段真实生成尾帧续接；同时存在多条硬切链时最多推进两条 |
| 已经创建 generation | “快速模式”以服务端冻结值为准；继续、失败重试和本地拼接重试均复用原选择，历史任务缺失该值时按关闭处理 |
| 长视频选择自动台词 | H3 分段音轨不进入成片；成片复用源音轨，长于画面时裁剪、短于画面时补静音，画面时长不变 |
| 长视频选择无台词 | H3 分段音轨不进入成片，最终输出静音视频 |
| 长视频确认画幅 | 检查 plan 绑定的硬切 first 与全部 end anchors；关闭快速模式的续接 first 使用上游真实生成尾帧，开启时使用 plan 中 SHA 绑定的冻结共享边界，不按当前源片段 source first 判断 |
| 长视频开始或继续生成 | 所有 segment 共用已冻结的画幅、清晰度和适配方式；关闭模式的上游真实尾帧、开启模式的冻结共享边界都按同一目标画幅处理 |
| H3 阶段某段确定失败 | 用户确认“重试生成”后使用新请求 id；服务端只复用状态成功且成片文件仍存在的段，并给出本次新增付费任务数 |
| 全部分段成功但本地拼接失败 | 即使会话级成片已半发布，仍展示成片与恢复区；用户确认“重试拼接”后复用原请求 id 和冻结输入，只在本地重拼；本次新增付费 H3 子任务为 0 |
| 某段提交结果未知 | 停止后续新提交并锁定整批，先去供应商核对，不允许自动或人工重提 |
| 历史 plan v1 含 11–15 秒段 | 只允许按已有 attempt 做 GET 恢复；缺少已有 attempt 时不创建新付费 POST |
| 校验 H3 分段原始输出 | receipt、输入与输出哈希必须匹配；成片不得比源片段目标短超过一帧，也不得比整秒请求长超过 1 秒 |
| 全部分段成功 | 归一化后按顺序拼接，校验总时长并原子写入会话级 `generated.mp4` |

## 边界

- 超过 300 秒、无法形成合法片段或台词跨越安全边界时，在任何付费请求前失败。
- 一期长视频不支持改写、翻译、编辑或自定义台词；这些模式返回明确错误，不静默降级。
- 拼接失败只重做本地拼接，不重新提交 H3。
- 原始分段输出的容差只用于复用已付费产物；最终拼接仍按源片段帧预算精确裁剪或补齐，并以全片时长验收。
- 首尾帧和连续性提示词只能提高连贯性，不承诺无缝或人物绝对一致；该链路不是供应商原生 extend。
- 当前网页只提供快速模式，首次新建长视频固定提交 `fast_mode=true`，不提供模式开关。后端继续兼容显式 `false`；已经创建的 generation 始终复用服务端冻结值，历史任务缺字段仍按关闭处理。

## 例子

- 输入：15 秒同镜头视频 → 输出：2 个 FL2VA 子任务和约 15 秒成片。
- 输入：从当前网页新建 30 秒视频 → 输出：网页固定提交快速模式，3 个子任务使用 plan 冻结共享边界、无需等待上一段成片即可快速提交；供应商可自行排队，最终仍按原顺序拼接。
- 输入：兼容调用显式提交 `fast_mode=false` → 输出：3 个按链顺序生成、用上一段真实尾帧续接的 FL2VA 子任务和约 30 秒成片。
