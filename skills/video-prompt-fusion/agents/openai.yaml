interface:
  display_name: "Video Prompt Fusion"
  short_description: "Fuse confirmed frames into ordered visual prose"
  default_prompt: "Use $video-prompt-fusion to fuse the four frozen input classes into ordered visual prose per frozen shot."
